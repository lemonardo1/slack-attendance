# 변경 사항

## 주간 근태 관리 기능 추가

### 새로운 기능

#### 1. 관리자 페이지 (`/stats`)
- **로그인 보호**: 환경 변수로 설정한 비밀번호로 접근 제어
- **주간 근태 테이블**:
  - 일요일부터 토요일까지 7일간의 근태 현황 표시
  - 각 사용자별 행으로 구성
  - 각 셀에 출근 시간, 퇴근 시간, 총 근무시간 표시
- **주간 탐색**: 이전 주, 이번 주, 다음 주 버튼으로 쉬운 탐색
- **세션 관리**: 로그인 후 24시간 동안 유지되는 세션

#### 2. 근무시간 자동 계산
- 출근과 퇴근 시간을 기반으로 총 근무시간 자동 계산
- 시간 단위로 표시 (소수점 1자리)

#### 3. 시각적 개선
- 반응형 디자인
- 출근(녹색), 퇴근(주황색) 색상 구분
- 주말(일요일, 토요일) 배경 강조
- 고정 헤더와 사용자 컬럼으로 큰 테이블도 쉽게 확인

### 기술적 변경사항

#### 새로운 파일
- `src/auth.ts` - 인증 관련 유틸리티
- `src/weeklyStats.ts` - 주간 통계 처리 로직
- `src/renderWeeklyStats.ts` - 주간 통계 UI 렌더링

#### 환경 변수
- `ADMIN_PASSWORD` 추가 (관리자 페이지 비밀번호)

#### 엔드포인트
- `GET /stats` - 주간 근태 관리 페이지 (로그인 필요)
- `POST /stats/login` - 로그인 처리
- `GET /stats/logout` - 로그아웃 처리
- `GET /` - 간단한 최근 기록 뷰 (로그인 불필요)

### 보안
- 세션 기반 인증
- HttpOnly, Secure, SameSite 쿠키 설정
- 비밀번호 검증

### 사용 예시
1. `https://your-worker.workers.dev/stats` 접속
2. 설정한 비밀번호 입력
3. 주간 근태 테이블 확인
4. 이전/다음 주 버튼으로 탐색

